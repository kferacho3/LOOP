import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import { Photo } from './photo';
import type { Program } from '@/lib/types';
import { cx } from '@/lib/utils';
export function ProgramCard({program,large=false}:{program:Program;large?:boolean}){return <Link href={`/programs/${program.slug}`} className={cx('group block',large&&'md:col-span-2')}><div className={cx('relative mb-5 overflow-hidden rounded-[20px]',large?'h-[340px] md:h-[470px]':'h-[340px] md:h-[470px]')}><Photo image={program.image} className="h-full w-full transition-transform duration-700 ease-liquid group-hover:scale-[1.035]" sizes={large?'(max-width:768px) 100vw, 50vw':'(max-width:768px) 100vw, 25vw'}/><span className="absolute top-5 left-5 rounded-full bg-paper px-3 py-2 text-[10px] font-semibold uppercase tracking-wider text-ink">{program.category}</span><div className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-black/85 to-transparent px-6 pt-24 pb-7 text-white"><h3 className={cx('max-w-sm leading-tight tracking-[-.04em]',large?'text-4xl':'text-3xl')}>{program.shortTitle}</h3><span className="mt-5 flex items-center justify-between gap-4 text-xs"><span>{program.title}</span><span className="liquid flex size-10 shrink-0 items-center justify-center rounded-full border border-white/40 group-hover:bg-paper group-hover:text-ink"><ArrowUpRight size={19} aria-hidden/></span></span></div></div></Link>;}
export function ProgramRows({programs,light=false}:{programs:Program[];light?:boolean}){return <div>{programs.map((p,i)=><Link key={p.slug} href={`/programs/${p.slug}`} className={cx('group liquid grid grid-cols-[32px_1fr_24px] items-center gap-5 border-t py-6 md:grid-cols-[48px_1fr_1fr_30px]',light?'border-paper/25 hover:bg-paper/5':'border-line hover:bg-linen/50')}><span className={cx('text-xs',light?'text-moss':'text-muted')}>{String(i+1).padStart(2,'0')}</span><h3 className="text-lg leading-snug tracking-[-.025em] md:text-xl">{p.title}</h3><p className={cx('hidden max-w-sm text-xs leading-6 md:block',light?'text-paper/75':'text-muted')}>{p.summary}</p><ArrowUpRight className="liquid group-hover:-translate-y-1 group-hover:translate-x-1" size={21} aria-hidden/></Link>)}</div>;}
