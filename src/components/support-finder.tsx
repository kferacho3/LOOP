'use client';
import { useState } from 'react';
import Link from 'next/link';
import { ArrowRight,Check } from 'lucide-react';
import { supportTypes } from '@/lib/validation';
import { cx } from '@/lib/utils';
export function SupportFinder(){const [selected,setSelected]=useState('');return <div className="rounded-[28px] border border-line bg-white/70 p-6 shadow-[0_20px_70px_-50px_#18251f55] sm:p-9"><p className="eyebrow mb-5 text-muted">Start with what matters to you</p><h3 className="text-3xl leading-tight tracking-[-.045em]">What would make today<br/>a little easier?</h3><p className="mt-4 text-sm leading-6 text-muted">Choose a starting point. You can tell us more on the next page.</p><div className="mt-7 flex flex-wrap gap-2" role="group" aria-label="Support interests">{supportTypes.map(t=><button type="button" key={t} aria-pressed={selected===t} onClick={()=>setSelected(t)} className={cx('liquid flex min-h-11 items-center gap-2 rounded-full border px-4 py-2 text-xs font-medium',selected===t?'border-forest bg-forest text-paper':'border-line bg-paper text-ink hover:border-forest')}>{selected===t&&<Check size={13} aria-hidden/>}{t}</button>)}</div><Link href={selected?`/get-help?need=${encodeURIComponent(selected)}`:'/get-help'} className="liquid mt-8 flex min-h-13 items-center justify-between rounded-full bg-ink px-6 py-4 text-sm font-semibold text-paper hover:bg-forest">{selected?'Continue with this support':'Explore support options'}<ArrowRight size={17} aria-hidden/></Link><p className="mt-4 text-center text-[11px] leading-5 text-muted">No account needed. No need to have it all figured out.</p></div>;}
