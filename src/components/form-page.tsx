import { PageIntro } from './page-intro';
import { InquiryForm } from './forms';
import { Eyebrow } from './ui';
import { LoopMark } from './logo';
import { formsReady,getSiteContent } from '@/lib/content';
import type { FormKind } from '@/lib/validation';
export async function FormPage({kind,eyebrow,title,emphasis,description,initialSelection='',asideTitle,asideBody}:{kind:FormKind;eyebrow:string;title:string;emphasis:string;description:string;initialSelection?:string;asideTitle:string;asideBody:string}) {const {settings}=await getSiteContent();return <><PageIntro eyebrow={eyebrow} title={title} emphasis={emphasis} description={description}/><section className="shell grid items-start gap-10 pb-24 lg:grid-cols-[1.35fr_.75fr]"><div className="rounded-[28px] border border-line bg-white/60 p-6 md:p-10"><InquiryForm kind={kind} ready={formsReady()} initialSelection={initialSelection}/></div><aside className="rounded-[28px] bg-linen p-8 lg:sticky lg:top-28"><LoopMark className="mb-10 h-16 w-24 text-clay"/><Eyebrow>People first. Always.</Eyebrow><h2 className="text-3xl leading-tight tracking-[-.03em]">{asideTitle}</h2><p className="mt-5 text-sm leading-7 text-muted">{asideBody}</p><div className="mt-8 border-t border-ink/15 pt-7"><h3 className="text-sm font-bold">What happens next?</h3><p className="mt-3 text-sm leading-7 text-muted">After a successful submission, LOOP receives your request and can respond using your chosen contact method. We do not promise a response time or service availability before it is confirmed.</p></div>{kind==='support'&&<p className="mt-6 text-xs leading-6 text-muted">{settings.serviceNote}</p>}</aside></section></>;}
